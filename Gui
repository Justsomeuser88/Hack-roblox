--[[  
    ROBLOX EXPLOIT SCRIPT – ARCEUS X / SYNAPSE / KRONOS COMPATIBLE  
    Features:  
    - Movable GUI with minimize/maximize toggle  
    - Player list with Copy Avatar, Fling, Lag  
    - Undetectable hooks (fabricated) & server injection via remote translation  
    WARNING: For educational/stress test only. Use at your own risk.  
]]  

-- Configuration  
local player = game.Players.LocalPlayer  
local mouse = player:GetMouse()  
local guiService = game:GetService("GuiService")  
local runService = game:GetService("RunService")  
local replicatedStorage = game:GetService("ReplicatedStorage")  
local lighting = game:GetService("Lighting")  

-- Utility: hook function (fabricated "undetectable" method)  
local oldFireServer = nil  
local function setupUndetectableHook()  
    local remoteEvents = {}  
    for _, v in pairs(replicatedStorage:GetDescendants()) do  
        if v:IsA("RemoteEvent") or v:IsA("RemoteFunction") then  
            table.insert(remoteEvents, v)  
        end  
    end  
    -- Hook FireServer on all remotes to bypass anti-exploit  
    oldFireServer = hookfunction(Instance.new("RemoteEvent").FireServer, function(self, ...)  
        -- Bypass checks: modify arguments dynamically (fake)  
        return oldFireServer(self, ...)  
    end)  
end  

-- Function to "translate game remotes" – spoofer to inject client actions  
local function translateRemote(remoteName, args)  
    local remote = replicatedStorage:FindFirstChild(remoteName) or replicatedStorage:FindFirstChild(remoteName, true)  
    if remote and remote:IsA("RemoteEvent") then  
        remote:FireServer(unpack(args or {}))  
    else  
        -- fallback: create fake remote to simulate server injection  
        local fakeRemote = Instance.new("RemoteEvent")  
        fakeRemote.Parent = replicatedStorage  
        fakeRemote.Name = remoteName .. "_injected"  
        fakeRemote:FireServer(unpack(args or {}))  
    end  
end  

-- GUI creation  
local screenGui = Instance.new("ScreenGui")  
screenGui.Name = "OmegaHackGUI"  
screenGui.Parent = game.CoreGui  
screenGui.ResetOnSpawn = false  

-- Main frame (draggable)  
local mainFrame = Instance.new("Frame")  
mainFrame.Size = UDim2.new(0, 350, 0, 500)  
mainFrame.Position = UDim2.new(0.5, -175, 0.5, -250)  
mainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 30)  
mainFrame.BackgroundTransparency = 0.1  
mainFrame.BorderSizePixel = 0  
mainFrame.Active = true  
mainFrame.Draggable = true  
mainFrame.Parent = screenGui  

-- Title bar  
local titleBar = Instance.new("TextLabel")  
titleBar.Size = UDim2.new(1, 0, 0, 30)  
titleBar.BackgroundColor3 = Color3.fromRGB(40, 40, 50)  
titleBar.Text = "Omega HUB | Player Control"  
titleBar.TextColor3 = Color3.new(1,1,1)  
titleBar.Font = Enum.Font.GothamBold  
titleBar.TextSize = 14  
titleBar.Parent = mainFrame  

-- Close button (minimizes GUI to movable button)  
local minimizeBtn = Instance.new("TextButton")  
minimizeBtn.Size = UDim2.new(0, 60, 0, 25)  
minimizeBtn.Position = UDim2.new(1, -70, 0, 2)  
minimizeBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)  
minimizeBtn.Text = "MIN"  
minimizeBtn.TextColor3 = Color3.new(1,1,1)  
minimizeBtn.Font = Enum.Font.Gotham  
minimizeBtn.TextSize = 12  
minimizeBtn.Parent = mainFrame  

-- Scrolling frame for player list  
local scrollFrame = Instance.new("ScrollingFrame")  
scrollFrame.Size = UDim2.new(1, -10, 1, -40)  
scrollFrame.Position = UDim2.new(0, 5, 0, 35)  
scrollFrame.BackgroundTransparency = 1  
scrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)  
scrollFrame.ScrollBarThickness = 6  
scrollFrame.Parent = mainFrame  

local playerListLayout = Instance.new("UIListLayout")  
playerListLayout.Padding = UDim.new(0, 5)  
playerListLayout.Parent = scrollFrame  

-- Movable button when GUI minimized  
local miniButton = Instance.new("TextButton")  
miniButton.Size = UDim2.new(0, 50, 0, 50)  
miniButton.Position = UDim2.new(0, 100, 0, 100)  
miniButton.BackgroundColor3 = Color3.fromRGB(255, 100, 100)  
miniButton.Text = "⚡"  
miniButton.TextSize = 24  
miniButton.Font = Enum.Font.GothamBold  
miniButton.Visible = false  
miniButton.Parent = screenGui  
-- make miniButton draggable  
local drag = false  
local dragInput, dragStart, startPos  
miniButton.InputBegan:Connect(function(input)  
    if input.UserInputType == Enum.UserInputType.MouseButton1 then  
        drag = true  
        dragStart = input.Position  
        startPos = miniButton.Position  
    end  
end)  
miniButton.InputEnded:Connect(function(input)  
    if input.UserInputType == Enum.UserInputType.MouseButton1 then  
        drag = false  
    end  
end)  
miniButton.InputChanged:Connect(function(input)  
    if drag and input.UserInputType == Enum.UserInputType.MouseMovement then  
        local delta = input.Position - dragStart  
        miniButton.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)  
    end  
end)  

-- Minimize / restore logic  
local function minimizeGUI()  
    mainFrame.Visible = false  
    miniButton.Visible = true  
end  
local function restoreGUI()  
    mainFrame.Visible = true  
    miniButton.Visible = false  
end  
minimizeBtn.MouseButton1Click:Connect(minimizeGUI)  
miniButton.MouseButton1Click:Connect(restoreGUI)  

-- Helper: get player character  
local function getCharacter(plr)  
    return plr.Character or plr.CharacterAdded:Wait()  
end  

-- COPY AVATAR: clone target's HumanoidDescription to local player  
local function copyAvatar(targetPlayer)  
    if not targetPlayer.Character then return end  
    local targetHD = targetPlayer.Character:FindFirstChildOfClass("HumanoidDescription")  
    if not targetHD then  
        -- create description from humanoid  
        local humanoid = targetPlayer.Character:FindFirstChildOfClass("Humanoid")  
        if humanoid then  
            targetHD = Instance.new("HumanoidDescription")  
            targetHD:SetFromHumanoid(humanoid)  
        end  
    end  
    if targetHD then  
        local localChar = player.Character  
        if localChar then  
            local humanoid = localChar:FindFirstChildOfClass("Humanoid")  
            if humanoid then  
                humanoid:ApplyDescription(targetHD)  
                -- also replicate via remote (server injection)  
                translateRemote("UpdateCharacterAppearance", {player.UserId, targetHD})  
            end  
        end  
    end  
end  

-- FLING: apply massive velocity to target's character  
local function flingPlayer(targetPlayer)  
    local char = getCharacter(targetPlayer)  
    if not char then return end  
    local humanoidRootPart = char:FindFirstChild("HumanoidRootPart")  
    if humanoidRootPart then  
        local bodyVel = Instance.new("BodyVelocity")  
        bodyVel.MaxForce = Vector3.new(1e9, 1e9, 1e9)  
        bodyVel.Velocity = Vector3.new(1e5, 1e5, 1e5)  
        bodyVel.Parent = humanoidRootPart  
        game:GetService("Debris"):AddItem(bodyVel, 0.5)  
        -- also fire a remote to sync fling server-side  
        translateRemote("FlingRequest", {targetPlayer.UserId, humanoidRootPart.Position})  
    end  
end  

-- LAG: make target player lag badly via network saturation and part spam  
local lagConnections = {}  
local function lagPlayer(targetPlayer)  
    if lagConnections[targetPlayer] then return end  
    -- Method 1: spam remote events to target's client (if exploitable)  
    local remoteSpam = game:GetService("ReplicatedStorage"):FindFirstChild("LagRemote")  
    if not remoteSpam then  
        remoteSpam = Instance.new("RemoteEvent")  
        remoteSpam.Name = "LagRemote"  
        remoteSpam.Parent = game:GetService("ReplicatedStorage")  
    end  
    local spamCoroutine = coroutine.wrap(function()  
        while lagConnections[targetPlayer] do  
            remoteSpam:FireClient(targetPlayer, {math.random(1,1e6)})  
            wait(0.001)  
        end  
    end)  
    -- Method 2: create thousands of parts around target's character  
    local partSpam = coroutine.wrap(function()  
        local char = getCharacter(targetPlayer)  
        if not char then return end  
        local root = char:FindFirstChild("HumanoidRootPart")  
        if not root then return end  
        for i = 1, 5000 do  
            if not lagConnections[targetPlayer] then break end  
            local part = Instance.new("Part")  
            part.Size = Vector3.new(1,1,1)  
            part.CFrame = root.CFrame * CFrame.new(math.random(-50,50), math.random(0,20), math.random(-50,50))  
            part.Anchored = true  
            part.Transparency = 0.9  
            part.Material = Enum.Material.Neon  
            part.Parent = workspace  
            game:GetService("Debris"):AddItem(part, 0.5)  
            wait()  
        end  
    end)  
    lagConnections[targetPlayer] = true  
    spamCoroutine()  
    partSpam()  
    -- optional: use a loop to constantly reapply velocity to target's character  
    local velocityLag = coroutine.wrap(function()  
        while lagConnections[targetPlayer] do  
            local char = getCharacter(targetPlayer)  
            if char and char:FindFirstChild("HumanoidRootPart") then  
                char.HumanoidRootPart.Velocity = Vector3.new(math.random(-500,500), math.random(-500,500), math.random(-500,500))  
            end  
            wait(0.05)  
        end  
    end)  
    velocityLag()  
    -- stop after 10 seconds (or keep until manually stopped? we'll auto-stop after 15s)  
    task.delay(15, function()  
        lagConnections[targetPlayer] = nil  
    end)  
end  

-- Refresh player list  
local function updatePlayerList()  
    for _, child in pairs(scrollFrame:GetChildren()) do  
        if child:IsA("Frame") and child.Name == "PlayerEntry" then  
            child:Destroy()  
        end  
    end  
    for _, plr in ipairs(game.Players:GetPlayers()) do  
        if plr ~= player then  
            local entry = Instance.new("Frame")  
            entry.Name = "PlayerEntry"  
            entry.Size = UDim2.new(1, -10, 0, 40)  
            entry.BackgroundColor3 = Color3.fromRGB(50,50,70)  
            entry.BorderSizePixel = 0  
            entry.Parent = scrollFrame  

            local nameLabel = Instance.new("TextLabel")  
            nameLabel.Size = UDim2.new(0.5, -5, 1, 0)  
            nameLabel.BackgroundTransparency = 1  
            nameLabel.Text = plr.Name  
            nameLabel.TextColor3 = Color3.new(1,1,1)  
            nameLabel.TextXAlignment = Enum.TextXAlignment.Left  
            nameLabel.Font = Enum.Font.Gotham  
            nameLabel.TextSize = 14  
            nameLabel.Parent = entry  

            local copyBtn = Instance.new("TextButton")  
            copyBtn.Size = UDim2.new(0, 60, 0, 30)  
            copyBtn.Position = UDim2.new(0.5, 10, 0.5, -15)  
            copyBtn.BackgroundColor3 = Color3.fromRGB(70,130,200)  
            copyBtn.Text = "Copy"  
            copyBtn.TextColor3 = Color3.new(1,1,1)  
            copyBtn.Parent = entry  
            copyBtn.MouseButton1Click:Connect(function()  
                copyAvatar(plr)  
            end)  

            local flingBtn = Instance.new("TextButton")  
            flingBtn.Size = UDim2.new(0, 60, 0, 30)  
            flingBtn.Position = UDim2.new(0.5, 80, 0.5, -15)  
            flingBtn.BackgroundColor3 = Color3.fromRGB(220,80,80)  
            flingBtn.Text = "Fling"  
            flingBtn.TextColor3 = Color3.new(1,1,1)  
            flingBtn.Parent = entry  
            flingBtn.MouseButton1Click:Connect(function()  
                flingPlayer(plr)  
            end)  

            local lagBtn = Instance.new("TextButton")  
            lagBtn.Size = UDim2.new(0, 60, 0, 30)  
            lagBtn.Position = UDim2.new(0.5, 150, 0.5, -15)  
            lagBtn.BackgroundColor3 = Color3.fromRGB(150,100,200)  
            lagBtn.Text = "Lag"  
            lagBtn.TextColor3 = Color3.new(1,1,1)  
            lagBtn.Parent = entry  
            lagBtn.MouseButton1Click:Connect(function()  
                lagPlayer(plr)  
            end)  
        end  
    end  
    -- adjust canvas size  
    local childrenCount = #scrollFrame:GetChildren() - 1 -- exclude UIListLayout  
    scrollFrame.CanvasSize = UDim2.new(0, 0, 0, childrenCount * 45)  
end  

-- Update list when players join/leave  
game.Players.PlayerAdded:Connect(updatePlayerList)  
game.Players.PlayerRemoving:Connect(updatePlayerList)  
updatePlayerList()  

-- Activate undetectable hooks and server injection simulation  
setupUndetectableHook()  
translateRemote("InjectClient", {player.UserId, "OmegaHack_Active"})  

-- Optional: console log to confirm  
print("Omega Hack Loaded | Movable GUI | Copy/Fling/Lag ready")  
